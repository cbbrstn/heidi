<div class="row">
  <div class="col s12">
    <%= form_for @webcam do |f| %>

    <div class="col s12 input-field">
      <%= f.text_field :name, id: 'first_name', class: "validate" %>
      <label for="name">Name</label>
    </div>

    <div class="col s12 input-field">
      <%= f.text_field :url, id: 'url', class: "validate" %>
      <label for="url">URL</label>
    </div>

    <div class="col s12 input-field">
      <%= f.text_field :location, id: 'location', class: "validate" %>
      <label for="url">Address</label>
    </div>

  <%= link_to "Back", root_path, class: "btn blue deep-purple darken-4 waves-effect waves-light" %>  <%= f.submit "Save", class: "btn waves-effect waves-light"%>
    <% end %>



  </div>

</div>
