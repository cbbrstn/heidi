<div class="row">
  <div class="col s6">
    <h3>Heidi</h3>
  </div>

  <div class="col s6 right-align">
    <br>
    <a href="<%= new_webcam_path %>" class="btn btn-floating btn-large cyan pulse"><i class="material-icons">videocam</i></a>
  </div>
</div>





<div class="collection">
  <% @webcams.each do |webcam| %>
<%= link_to "#{webcam.name} (#{webcam.location})", webcam_path(id: webcam.id), class: "collection-item" %>
<% end %>
</div>
